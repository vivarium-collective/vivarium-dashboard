---
name: streamline-chain-dispatch-job-cancellation
priority: high
optional: false
id: item-53
---

## Goal

A real, production-grade way to cleanly cancel an in-flight chain-dispatch campaign — one API
call or one UI button, not the hand-rolled multi-step process this session had to improvise live
against real production infrastructure.

## Why this is needed

Real, live incident, 2026-08-14/15: had to stop a real 1000-seed chain-dispatch campaign
(backlog item 1) after discovering its wall-time made it unviable, then discovered (and had to
also stop) two further accidental duplicate campaigns caused by item 51's own root-cause bug
(the workbench client's 30s timeout falsely reporting failure while the real dispatch keeps
running server-side — each retry silently created a full independent campaign).

**What actually had to be done, live, with no existing tooling for it**:
1. `aws batch cancel-job` does **not** work on jobs that are PENDING due to an unmet
   `dependsOn` dependency (confirmed empirically — the API call returns success, but the job's
   status never changes, even after 20+ seconds). It **does** work on RUNNABLE jobs (dependency
   already resolved, just waiting on capacity).
2. The only reliable per-job lever for a dependency-blocked chain is `terminate-job` on whichever
   job is currently RUNNING — which, because of the architecture's own dependency-cascade design
   (a FAILED job auto-fails everything that depends on it), kills that seed's entire remaining
   chain in one call. But because the compute environment keeps promoting new PENDING(capacity)
   jobs to RUNNING as fast as capacity frees up, a single pass isn't enough — required a
   **repeated sweep loop** (list RUNNING → terminate all → wait → repeat) until real, sustained
   convergence to zero, taking dozens of iterations across a real 1000-seed campaign.
3. Even full convergence to "0 running" is **not durable** — any seed whose current job was
   RUNNABLE-but-never-yet-scheduled would resume the instant the sweep stopped. The only real,
   structural stop is disabling the **entire compute environment** (`--state DISABLED`) — a
   queue-wide lever that blocks every OTHER dispatch shape too (canonical, comparison-ensemble,
   phase0), not scoped to the one campaign being cancelled. Confirmed this blocked coworkers from
   using the fully-remote GovCloud workbench/viva-api entirely while it was in effect.
4. Re-enabling the CE after a `maxvCpus` fight (an update call that silently didn't apply — real
   value drifted to a wrong intermediate number across several attempts) required its own
   verification pass to confirm the intended capacity was actually restored, not just that the CE
   was `ENABLED`.

**None of this should require a human hand-rolling AWS Batch API calls against live production
infrastructure during an incident.** A real fix needs, at minimum: (a) a single, real
"cancel this campaign" operation — server-side, idempotent, that correctly walks a campaign's own
tracked job ids (the `HpcRun.chain_final_job_ids` mechanism already used for completion tracking)
and cancels/terminates each appropriately by its real current state, without a human needing to
know the cancel-vs-terminate distinction; (b) this exposed as a real UI affordance (a "Stop
campaign" button, mirroring the existing single-job "Stop control" already shipped for the
non-chain path — see `.todo/_backlog.md`'s own item on "Stop control to recover a frozen
in-flight run") so an operator never again needs shell access to production AWS credentials to
stop a mistaken dispatch; (c) should NOT require disabling the whole compute environment — a
correctly-implemented per-campaign cancel should never need a queue-wide blast radius.

Directly related to item 51 (the false-failure bug whose retries caused the real incident this
item is grounded in) — fixing 51 reduces how often cancellation is needed; this item makes
cancellation safe and fast when it inevitably still is.

---

## Status: DESIGN GROUNDED, NOT YET IMPLEMENTED

**Description**: Filed 2026-08-15, same session as the real incident that motivated it. Started
real design investigation the same day; deliberately stopped short of implementing, for a real
reason explained below — not stalled, a judgment call.

### Real design finding, verified against live data (2026-08-15)

`HpcRun.chain_final_job_ids` (`viva-api/viva_api/simulation/models.py`) tracks only each seed's
**last** generation job id, not every job in its chain. For a mid-flight campaign, that final job
is almost always still `PENDING` (blocked on its own upstream dependency) — not the job that's
actually cancellable. Verified live (not guessed) that each chain job's own `dependsOn` field
correctly points at its real predecessor:
```
chain-seed0-gen1-...  dependsOn -> chain-seed0-gen0-... (real job id, confirmed via describe-jobs)
```
This means a real cancel mechanism can walk backward from the tracked final id through each job's
own `dependsOn` link — batched `describe_jobs` calls (100 ids/call, existing
`_DESCRIBE_JOBS_MAX_BATCH` pattern already used by `get_chain_campaign_result`) — until it finds,
per seed, the one job that's actually non-terminal-and-actionable, and acts on THAT one; Batch's
own dependency-cascade (already relied on elsewhere in this design) fails everything downstream
automatically. No new DB schema needed — this reuses `chain_final_job_ids` as the walk's starting
point.

**Real, unresolved ambiguity found doing this — the reason implementation is paused, not the
reason it's stalled**: tonight's own empirical incident-response finding was that `cancel-job`
does NOT work on a job `PENDING` due to an unresolved `dependsOn` (confirmed live, real API calls,
multiple times). But AWS's own `cancel-job` documentation says it works on jobs in `PENDING` OR
`RUNNABLE` state. These aren't necessarily contradictory — `PENDING` likely covers two genuinely
different sub-cases (freshly submitted vs. blocked-on-a-dependency) that share one status string —
but nothing in `describe-jobs`'s output cleanly labels which sub-case a given `PENDING` job is in.
The safest real signal available is checking the job's OWN `dependsOn` target's status (terminal or
not) rather than trusting the `PENDING` string alone — but this needs real, live verification
against actual jobs in each state before it's trustworthy, not just reasoned through.

**Why this is deliberately not rushed**: this is exactly the tool that gets reached for during a
real, stressful incident (tonight's own). A cancel mechanism that's subtly wrong in a way that
looks like it worked (a job it can't actually act on, silently no-op'd) would be *worse* than the
current hand-rolled-but-understood sweep-loop process — it would give false confidence during
exactly the moment that confidence matters most. This needs proper implementation time with real
per-state empirical verification (submit a few tiny real test jobs in each Batch state, confirm
cancel/terminate behavior against each directly), not a rushed pass under time pressure.

### What's left to build, once real time is available

1. Verify the `PENDING` sub-case ambiguity empirically (real, tiny test jobs — cheap).
2. Implement the walk-back-and-act function in `viva-api` (`simulation_service_ray.py`, alongside
   `get_chain_campaign_result`), with real tests against each job state.
3. A real endpoint exposing it, reusing `chain_final_job_ids` — no new tracking needed.
4. A real UI "Stop campaign" button in vivarium-workbench, mirroring the existing single-job Stop
   control.
5. Should NOT require disabling the whole compute environment — the per-seed walk-back should be
   sufficient on its own; validate this holds under a real (small-scale) test campaign before
   trusting it for a real 1000-seed one.

## Explicitly deferred past item 1, 2026-08-16 — Alex's own words

"definitely defer past item 1, unless absolutely needed. i just want to have a clean way to cancel
something like the 1000x10 and not have the same issue we last had. (perhaps the backlog item 40
endpoint would be the solution if not a heavy/complex lift?)" Item 40 is being investigated now
(concurrent agent) specifically to answer whether it alone satisfies the real want here, without
needing this item's own larger design. This item stays paused unless that investigation shows it's
genuinely unavoidable — not to be started otherwise without a fresh, explicit go.

## Item 40's investigation RETURNED, 2026-08-16 — answer: item 40 alone is NOT sufficient

Traced the real code path end to end (not inferred): `cancel_simulation` →
`SimulationServiceRay.cancel_job` resolves exactly one `HpcRun.job_id_ext` and terminates exactly
that one AWS Batch job — never `chain_final_job_ids`, no walk through `dependsOn`. For a
chain-dispatch campaign, that tracked `job_id_ext` is the **ParCa job's id**, which is almost
always already terminal by the time a mid-flight campaign needs cancelling — so item 40's fix
(real and needed on its own merits — see `.todo/backlog/40.md`) is very likely a no-op against an
already-finished job for exactly the scenario Alex asked about, while the campaign's real N×G
per-seed compute keeps running untouched. This directly confirms this item's own original design
finding (`chain_final_job_ids` only tracks each seed's LAST, usually-still-PENDING job) rather than
superseding it.

**This is now a live, unresolved question for Alex, not a decision made on this item's behalf** —
per the standing instruction attached to this deferral, this finding must be surfaced, not acted
on unilaterally. Item 53 stays in `DESIGN GROUNDED, NOT YET IMPLEMENTED` status (top of this file)
unless/until Alex gives a fresh, explicit go — the "unless absolutely needed" condition in his own
deferral is now genuinely met by real evidence, but that's his call to make, not an automatic
trigger.

## Step 1 (PENDING-ambiguity empirical verification) RETURNED, 2026-08-16 — hypothesis CONFIRMED and refined

Stream D (`afeec2367414893d4`) completed the cheap-verification-only task. Used `smsvpctest`'s own
Batch queue (`smsvpctest-vecoli-task-amd64`, same account/region as prod but structurally separate
from `smscdk-vecoli-task-amd64`) — never touched prod. Confirmed zero active jobs before and after;
full cleanup verified via final `describe-jobs`/`list-jobs` sweeps.

**Confirmed, more precisely than the original incident writeup**: a *sustained/observed* `PENDING`
status reliably means "blocked on an unresolved `dependsOn`." Jobs with no dependency or an
already-resolved one do NOT durably occupy `PENDING` at all — they pass through `SUBMITTED` for a
few seconds and go straight to `RUNNABLE`, skipping `PENDING` entirely (confirmed across 4
independent test jobs polled every 0.3-0.5s; `PENDING` never once observed for this case). The
distinguishing signal is exactly what this file's own design already proposed: cross-reference a
`PENDING` job's `dependsOn[].jobId` against that dependency's own current status — `statusReason`
on a blocked-PENDING job is an empty string, ruled out as a signal.

**New, operationally important refinement** (does not contradict the original incident finding,
sharpens it): `cancel-job` on a blocked-PENDING job IS accepted (`isCancelled: true` appears
immediately, API returns success) but its effect is **deferred**, not dropped — the job stays
PENDING, unaffected, for as long as the dependency stays unresolved (confirmed unchanged for 50+
continuous seconds), and only resolves to `FAILED` (using the original cancel `--reason`) the
moment the dependency itself reaches a terminal state. If the blocking dependency never resolves,
the cancellation sits dormant forever — exactly the runaway-campaign scenario this item exists for.
This directly confirms (not just theoretically, empirically) that this item's own walk-to-the-
actually-actionable-job design remains correct: relying on Batch's own dependency cascade-fail
alone is not sufficient; the walk-back function needs to find and act on whichever job is currently
RUNNABLE (cancel, takes effect within ~1 poll interval, confirmed) or RUNNING (needs `terminate-job`,
NOT `cancel-job` which is silently a no-op on RUNNING; confirmed real propagation delay of
~30-40s before a terminated job actually flips to FAILED/exitCode 137 — a real walk-back mechanism
must tolerate this, not assume immediacy).

**Caveats, honestly reported by Stream D, not rounded up**: no stable/catchable PENDING sub-case
was found for a resolved-or-absent dependency (looked across 4 jobs at 0.3-0.5s polling) — cannot
fully rule out a sub-second tick invisible to CLI polling, but not operationally relevant if so.
Did not test other hypothetical causes of sustained PENDING (fairshare throttling, malformed
resource requests, disabled queue) — out of scope. Full raw evidence (81 per-poll JSON snapshots,
full CLI transcript) left in scratchpad, not committed anywhere (verification-only, no PR/commit
per its scope).

**Status unchanged**: item 53 remains `DESIGN GROUNDED, NOT YET IMPLEMENTED` — this was verification
only, not authorization to build. The full walk-back function/endpoint/UI button remain a separate,
explicit future decision.

## Alex's decision, 2026-08-16 — "Cheap verification only"

Presented 3 options (cheap-verification-only / start full build now / keep deferring). Alex chose
**cheap verification only**: dispatch just this file's own step 1 (verify the `PENDING` sub-case
ambiguity empirically via real, tiny AWS Batch test jobs) — explicitly NOT the full 5-step build
(walk-back function, endpoint, UI button, campaign-scale validation), and NOT a return to pure
deferral either. A fresh `general-purpose` agent dispatched (Stream D, `afeec2367414893d4`) with a
narrow, explicit scope: determine whether "PENDING" genuinely covers two sub-cases (freshly-
submitted vs. dependency-blocked) sharing one status string, and if so what real `describe-jobs`
field distinguishes them — using the safest available AWS environment (checking `smsvpctest`'s own
Batch setup before considering the real Stanford GovCloud prod queue), minimal/tagged/cheap test
jobs, full cleanup required, explicitly barred from implementing any of the remaining 4 build steps.
Not yet returned as of this note.

---

## Real investigation + design pass, 2026-08-17 — a cancel endpoint ALREADY EXISTS (broken for
## chain campaigns); precise fix specified below; NOT implemented — a real tooling constraint
## blocked committing it, explained at the end

Dispatched as (t-b) of the item-1 6-step plan, after the CDK/SSO blocker cleared. Re-reads this
item's own prior design work above (still valid, not superseded) and extends it with what real
source-reading found.

**Correction to the plan's own framing**: `POST /api/v1/simulations/{id}/cancel` does not need to
be *built* — it already exists, real route is actually `DELETE /simulations/{id}/cancel`
(`viva-api/viva_api/api/routers/sms.py:380-406`, `operation_id="cancel-ecoli-simulation"`), backed
by `common/handlers/simulations.py:870-896`'s `cancel_simulation()`. Follow the real, established
convention (`DELETE`, matching REST semantics for "stop the in-flight thing") rather than adding a
new `POST` route — this ecosystem's own design philosophy is reuse-before-invent.

**The real, confirmed gap — this is what item 1's tear-down had to hand-roll around all session**:
`cancel_simulation()`'s current logic:
```python
hpc_run = await db_service.get_hpcrun_by_ref(ref_id=simulation_id, job_type=JobType.SIMULATION)
...
service = get_simulation_service_for_job(hpc_run.job_id) or simulation_service
await service.cancel_job(hpc_run.job_id)          # <-- ONE job id only
update = JobStatusUpdate(job_id=hpc_run.job_id, status=JobStatus.CANCELLED)
await db_service.update_hpcrun_status(hpcrun_id=hpc_run.database_id, update=update)
```
`hpc_run.job_id` is a single `JobId` — for a chain-dispatch campaign, the real per-seed job ids
live in `hpc_run.chain_final_job_ids` (a `list[str]`, one per seed's *last* generation — see this
item's own prior design section above for why that's the right walk-back starting point). This
handler never reads that field at all. Calling this endpoint today on a real chain campaign cancels
at most one seed's current job — not the campaign — which is exactly why this session's real
teardown (sim165, ~1000 jobs, and again sim166, ~9975 jobs) needed a hand-rolled repeated
`terminate-job` sweep loop instead. `SimulationService.cancel_job()` itself
(`simulation_service_ray.py:1607-1614`) is fine as-is — `self._batch().terminate_job(...)`
correctly kills a job plus its child MNP nodes; the gap is purely that the handler above never
calls it per-seed for a chain campaign.

**Precise proposed fix** (design-complete, NOT implemented — see blocker below):

In `common/handlers/simulations.py`, extend `cancel_simulation()`:
```python
if hpc_run.chain_final_job_ids:
    # Chain-dispatch campaign: walk every seed's own dependsOn chain (see this
    # item's design section above) to find, per seed, the one actually
    # non-terminal/actionable job -- not just the tracked final id, which is
    # usually still PENDING on its own upstream dependency mid-campaign.
    # Batch describe_jobs in chunks of _DESCRIBE_JOBS_MAX_BATCH (100), same
    # pattern get_chain_campaign_result already uses one function up.
    results = await service.cancel_chain_campaign(hpc_run.chain_final_job_ids)
    # results: per-seed outcome (cancelled / already-terminal / failed-to-cancel)
    # -- do NOT collapse partial failure into a blanket success (this project's
    # own standing rule: never silently swallow partial failure). If any seed's
    # job could not be terminated, surface that in the response, not just log it.
else:
    await service.cancel_job(hpc_run.job_id)   # existing single-job path, unchanged
```
New `SimulationServiceRay.cancel_chain_campaign(job_ids: list[str]) -> ChainCancelResult` (new
method, alongside `get_chain_campaign_result` in `simulation_service_ray.py`): for each seed's
tracked final id, walk backward via `dependsOn` (batched `describe_jobs`, exactly
`get_chain_campaign_result`'s existing chunking) to the first non-terminal job in that chain, then:
- if `RUNNING` → `terminate_job` (kills it, Batch's own dependency-cascade fails everything
  downstream in that seed's chain automatically — already relied on elsewhere in this design).
- if `RUNNABLE` → `cancel_job`.
- if `PENDING` → see the CORRECTION immediately below — this is NOT actually an open question.
- if already terminal (`SUCCEEDED`/`FAILED`) → no-op, record as already-done (idempotency).

DB write: once ALL seeds are confirmed terminal-or-cancelled, write `JobStatus.CANCELLED` via the
existing `update_hpcrun_status`. Idempotency: the existing `hpc_run.status in (COMPLETED, FAILED,
CANCELLED)` short-circuit at the top of `cancel_simulation()` already covers "don't re-act on an
already-terminal simulation" — no change needed there, it works identically for chain and non-chain
rows.

**CORRECTION, applied at the same checkpoint this section was written — two of this pass's own
claims were stale, not new findings, due to a real tooling constraint (see blocker below) that
blocked it from checking things a same-day-but-earlier investigation had already settled:**

1. *"Item 40 not confirmed merged"* — **wrong, already resolved 2026-08-16** (see `.todo/backlog/
   40.md`'s "MERGED, 2026-08-16" section, written before this investigation ran). Independently
   re-verified directly against `viva-api`'s real git history at this checkpoint:
   `ec75e4150285e181263f0860d67bb41bbc9f8c8f` (PR #250) IS a real ancestor of `main`, migration
   `44335812e447` is present. The chain-dispatch fix above can proceed without waiting on this.
2. *"PENDING-vs-dependsOn empirical question still open"* — **wrong, already answered 2026-08-16**
   by Stream D's real test (see "Step 1... RETURNED" section above, same file): `cancel-job` on a
   dependency-blocked `PENDING` job IS accepted immediately (`isCancelled: true`) but the effect is
   **deferred, not dropped** — the job stays `PENDING` until its dependency resolves, then flips to
   `FAILED` with the original cancel reason. Confirmed unchanged for 50+ continuous seconds in that
   test. This means the walk-back design's fallback ("wait one poll cycle") was directionally
   right but understates it — a `PENDING` job does NOT need special handling at all beyond calling
   `cancel_job` on it like any other non-terminal job; Batch itself defers the effect correctly.
   `RUNNING` still separately needs `terminate_job`, not `cancel_job` (confirmed a silent no-op on
   `RUNNING` in that same test), with a real ~30-40s propagation delay before the job flips to
   `FAILED`/exit 137 — the walk-back implementation must tolerate that lag, not assume immediacy.

Both corrections mean **`cancel_chain_campaign`'s design is actually simpler than this pass
proposed**: no genuinely open empirical question remains before implementation — `cancel_job` on
`RUNNABLE`/`PENDING`, `terminate_job` on `RUNNING`, no-op on already-terminal, is a complete,
already-validated state machine. The "first empirical step for whoever implements this" that this
pass deferred is not actually needed as a prerequisite anymore.

**Real blocker on actually delivering a PR**: dispatched as a worktree-isolated fork rooted in
*vivarium-workbench* (this repo) — but this task's actual code lives in the separate *viva-api*
repo. The harness's worktree isolation is hard-enforced per-repo: every attempt to run `git`
against `/Users/alexanderpatrie/sms/ecosystem/viva-api` (via `cd`, via `-C`, read-only `git log`
included) was refused outright with "a worktree-isolated agent's git operations must target its
own worktree." This is a real, structural limitation of how this investigation was dispatched, not
a decision — the design above is complete and precise enough to implement directly, and (per the
correction above) has zero remaining open empirical prerequisites. It needs either a fresh dispatch
rooted in viva-api itself (not vivarium-workbench, and without `isolation:'worktree'` given that
setting's own demonstrated cross-repo-rooting risk), or a human/agent with a real viva-api checkout
applying this spec directly.

**Status: DESIGN COMPLETE, STILL NOT IMPLEMENTED, ZERO REMAINING OPEN QUESTIONS.** Ready to hand
directly to a real implementation pass. TOC.md left unchecked (item 53 is not done); the gap
between "design complete" and "done" is now purely implementation.
