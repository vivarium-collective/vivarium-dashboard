---
name: ptools-variant-condition-metadata
priority: critical
optional: false
id: item-12
---

## Goal

The endpoint(s) in viva-api being called by the Pathway Tools client (that which are for the purpose of obtaining the ptools_*.tsv analyses
artifacts) to reflect the following new "feature-request":

"""
Thanks for this, but how can I tell what conditions each variant corresponds to? I have experimental datasets for 0 mecillinam and for 1.0 mg/mL mecillinam, but I don't know which variant best corresponds to these.
[1:24 PM]The API is also going to need to provide the info that the user will need to select a variant, but for now I just need to know the answer!
Suzanne Paley  [1:32 PM]
Never mind, I see that Rene has specified variants 1 and 4 for now. But the API will need to provide this info in the future.
A. Patrie  [1:42 PM]
Okay. As for the corresponding conditions, that would be a question for the modeling team. My guess is that we can reference the simulation's configuration to glean this condition-variant mapping.
[1:44 PM]You said "The API is also going to need to provide the info that the user will need to select a variant, but for now I just need to know the answer!" and "the API will need to provide this info in the future": this is great, but unclear in terms of what you are requesting from us. Do you need a new endpoint that provides such metadata, or do you expect this to be in your canonical analysis request.
Suzanne Paley  [2:56 PM]
I'm looking in the variants section of the metadata for this simulation, and there are 3 fields that have values arrays: times has 1 value, knockouts has 2 values, and concentrations has 6 values. So it makes sense that there are 1x2x6=12 variants, but it is not obvious from any of this information which variant number corresponds to which combination of values. 
I just need a way to reliably figure that out, so that I can ask for the data for a particular variant of interest.
"""

## Why this is needed

This is a feature request that cannot be ignored, as feature requests from the pathway tools developers is fundamentally critical
to the deliverable.

---

## Status: PENDING

**description**: Not yet picked up.
