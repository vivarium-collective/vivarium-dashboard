---
name: compose-v1-redesign-comprehensively-recorded
priority: medium
optional: false
id: item-50
n-agents: 5
fork: false
background: true
---

## Goal

The /compose/v1/* redesign as comprehensively recorded

## Why this is needed

This item is the comprehensive, standalone record of the `/compose/v1/*` canonical-dispatch
redesign — reproduced in full below from its two source documents (rather than left as pointers)
so this item alone is sufficient to resume the work. Origin: discovered while root-causing item
35's pilot dispatch bug (an ad-hoc routing conditional in viva-api), escalated by Alex into a
standing architectural mandate once the routing bug itself was fixed. Companion item: `49.md`
(the original, narrower routing-decision-fix scope, now superseded by the fuller migration scoped
here). Design doc of record: `viva-api/docs/DESIGN-process-bigraph-native-dispatch.md`. Standing
memory of record: `~/.claude/projects/-Users-alexanderpatrie/memory/viva-api-generalization-strategy.md`.

**Status as of last update**: fully scoped (three independent research streams, each verified
against real source, not planning-doc-trusted), a recommendation given to Alex (Path A), and
explicitly DEFERRED to a future session — not started, no branch, no code, no PR.

**UPDATE, 2026-08-14**: Alex has named this item as next-session priority #2 (after item 1), with a
new, specific first step before any implementation: a **comprehensive revision and review of this
plan against `vivarium-workbench` PR #805** ("docs(spec): dual-engine comparison — cross-environment
investigations", Jim Schaff, OPEN as of 2026-08-13) — a real, separate design spec for a sms-ecoli↔
vEcoli comparison harness whose own work item W4 is explicitly viva-api-owned (dependent-Batch-job
dispatch for a compare pipeline), territory that directly overlaps this item's own `/compose/v1/*`
scope. Open question for that review, not pre-judged: does PR #805's dispatch shape fit as evidence
FOR building this item's Path A now, or is it a separate effort that only happens to share a
subsystem? Full context: memory `next-session-direction-item1-and-backlog50-pr805`. Still not
started — no branch, no code, no PR — this is scoping-of-the-scoping, not implementation.

**This review is now done — see Part 3 below.** Short answer: partial yes, with a concrete
scope addition (a 5th Path A gap), not a clean yes/no.

---

### Part 1 — Standing strategic context (reproduced in full from memory: `viva-api-generalization-strategy.md`)

#### The naming lineage (Alex's own framing, verified consistent with real sources)

"Vivarium" (Agmon/Spangler/Ryan's original framework, Agmon et al., *Bioinformatics* 2022 —
"Vivarium 1.0") predates and is the informal/conversational name for what's now formally
"process-bigraph" ("Vivarium 2.0" — Agmon & Spangler, arXiv:2512.23754, 2026). Alex's own analogy:
"government name" vs. the name everyone actually uses, like a rapper's stage name. Confirmed
independently via this same design review's own theory research — the 2026 paper cites the 2022
paper as its direct predecessor.

`sms-api` was renamed to `viva-api` (`vivarium-collective/viva-api` PR #208, commit `1eb1b328`) as
a deliberate play on "vivarium-api," following the same precedent as `vivarium-dashboard` →
`vivarium-workbench`. The rename commit itself only documents mechanics (package rename,
back-compat shim) — the strategic reasoning lives in real planning docs, not the commit message.

#### The real strategic claim, verified against primary sources 2026-08-14

Alex's account: the SMS/ecoli deliverable (this whole ecosystem, workbench as stakeholder
entrypoint) is the "maiden voyage" of a long-planned trajectory where `pbg-template`/`viva-template`
generalizes to wrap ANY valid arbitrary simulator/computational model, any domain — not just
biology — compiled into process-bigraph. When the SMS project ends: (a) stakeholders get the
SMS-specific instance (v2ecoli/sms-ecoli via pbg-template) as proof the generalized abstraction
works; (b) viva-api itself will be able to run ANY viva-wrapped valid repo, the same way the
workbench runs v2ecoli today — possibly a different deployment (not GovCloud specifically, maybe
public). "viva-api seeks to serve process-bigraph as a whole, not just domain-specific ecoli
repos" — while still having "its own unique nomenclature, patterns, paradigms" (not a pure 1:1
passthrough of process-bigraph's raw API).

**Verified true, not just asserted** — real, dated planning docs (`viva-api/docs/PRE-DEMO-PLAN.md`,
`PRE-DEMO-MASTER-PLAN.md`, both last touched 2026-08-07) state this directly:
- *"Generalization is the hinge to the grand design, not a bonus."*
- *"Requirement: generalized + production-grade for ANY composite in ANY pbg-template workspace;
  v2ecoli is use-case #1."*
- Repeated *"generalized, not v2ecoli-shaped"* / *"any workspace"* framing throughout.

**And it's not aspirational — real, current, working code already embodies the split**:
- `/api/v1/simulations` (`viva_api/api/routers/sms.py`) — the vEcoli-ensemble-specific
  specialization: its own parameter surface (`simulator_id`/`num_generations`/`num_seeds`/
  `run_parca`/etc.), its own per-seed chain-dispatch machinery (`simulation_service_ray.py`'s
  `submit_chain_dispatch_job`, item 33's checkpoint/resume design). **Deliberately, explicitly NOT
  meant to converge with the generic path** — verified directly: *"We do NOT converge on the
  `/api/v1/simulations` endpoint... but we DO reuse the Batch machinery beneath it... Generic
  `run_pbg.py` is the driver for any composite; the vEcoli ensemble driver is the specialization."*
  (`PRE-DEMO-PLAN.md:130`)
- `/compose/v1/*` (`viva_api/api/routers/compose.py`) — the ALREADY-REAL generic path. Drives
  `run_pbg.py`'s domain-agnostic `Composite(doc).run(steps)` against ANY composite document.
  `viva_api/compose/simulation_service_ray.py`'s `ComposeSimulationServiceRay` (docstring: *"Runs
  the GENERIC run_pbg.py runner on the same Batch MNP machinery... instead of the vEcoli ensemble
  driver"*) and `viva_api/compose/simulation_service.py`'s `ComposeSimulationServiceHpc` (SLURM
  equivalent) mirror the SAME per-backend registry pattern (`_init_simulation_service`'s
  `dict[ComputeBackend, SimulationService]`) the vEcoli ensemble path pioneered.

#### How this should shape future viva-api design work

Any dispatch/routing/architecture proposal for viva-api should ask: does this belong in the
vEcoli-ensemble specialization (`/api/v1/simulations` family — legitimate to keep vEcoli-specific
concepts here) or the generic compose path (`/compose/v1/*` — must stay domain-agnostic, composite
is the only source of truth)? Don't collapse the two — that's an already-made architectural
decision, not an open question.

#### MAJOR ESCALATION, 2026-08-14 — the new standing goal

Alex confirmed and sharpened this further: `/api/v1/simulations` is explicitly BACK-COMPAT ONLY.
**The canonical fully-remote Ray-backed dispatch (item 35's real shape) MUST run through
`/compose/v1/*`, non-invasively** (reuse existing machinery, don't duplicate/rewrite what works).
His own words: "This is like the big bang in terms of primitive fundamentals that are core to this
project" — and "viva-api == vivarium-api, so vivarium-api, vivarium-workbench, vivarium-template,
etc. Its a customizable ecosystem that can be modularly implemented and used by any multi-repo,
research-based organization" — i.e. the whole ecosystem, not just viva-api, is meant to be a
reusable platform, not SMS/ecoli-specific tooling that happens to be reused.

He explicitly wants the RIGHT fix, not a patch — but asked for an honest time-cost assessment first
("only if that's not a huge time sync") and a recommendation before committing to anything.

**Research stream 1/3 (real current `/compose/v1/*` capabilities), verified line-by-line, not
planning-doc-trusted**: neither compose backend (Ray or HPC) submits more than one job per call
today; zero dependency-chain vocabulary anywhere in the router/handler/model stack
(`ComposeSimulationRequest`/`ComposeHpcRun` have no seeds/generations/chain fields at all). BUT:
`_submit_mnp`'s `depends_on=[...]` capability is mechanically reachable in ONE LINE from
`ComposeSimulationServiceRay` — it already holds a real `SimulationServiceRay` instance
(`self._ray`, not a reimplementation), and `_submit_mnp` (with `depends_on`/`depends_type`/
`retry_strategy`) is a single shared implementation, not duplicated. `run_pbg.py`'s
`--composite-id`/`--overrides` mode (the generic runner) ALREADY supports what
`_seed_generation_command` needs — only the specific composite-id/override key names are
v2ecoli-specific, not the mechanism. `_init_compose_subsystem` already has a real per-backend
registry (the `PRE-DEMO-PLAN.md` "still hardcoded" claim is now STALE, verified against current
source — already fixed).

What's genuinely legacy-specific, not reusable as-is: campaign tracking fields
(`chain_n_generations`/`chain_final_job_ids`) exist only on legacy `HpcRun`, not `ComposeHpcRun`;
`JobScheduler.update_chain_campaigns` (the fan-in poller) is hard-typed to concrete
`SimulationServiceRay`; `ComposeJobMonitor` has zero chain-related methods; the data model
(`Simulation`/`SimulatorVersion`/`ParcaDataset`) is structurally separate from compose's own types.

Agent's own conclusion, well-reasoned: this is a genuine "extend an existing pattern" case (this
repo's own design-coordinates step 2), not step-1 (trivial reuse) or step-4 (invent new). Needed: a
compose-side chain-submit method (reusing the already-shared primitives, not duplicating them), a
place to persist per-seed job ids, a fan-in poller (new or generalized). **No new sms-cdk
infrastructure, no new job type, no new low-level submission mechanism needed** — orchestration/
wiring work, not new execution machinery. This is a real, bounded, well-scoped software task, not
an open-ended rewrite.

**Research stream 2/3 (line-by-line generic-vs-vEcoli-specific classification), even more
precise**: confirms stream 1, narrows the real gap to exactly FOUR concrete places, verified
against real source, not inferred:
1. Where `n_seeds`/`n_generations`/composite identity are SOURCED from — viva-api's own ORM
   (`Simulation.num_seeds`) + a hardcoded `V2ECOLI_BATCH_BASELINE_COMPOSITE_ID` constant, instead
   of the composite's own `CompositeSpec.parameters` (which already has both, proven by
   `batch_baseline`).
2. The override-KEY VOCABULARY (`cache_dir`, `seed`, the checkpoint triple, etc.) is
   hardcoded as literal strings in `_seed_generation_command`, even though these ARE the
   composite's own real declared parameter names — needs to read them off the resolved spec
   instead of hardcoding. (Corrected 2026-08-16: this key was `base_seed`, renamed to `seed` by
   item 55's fix — gap itself unaffected, just the example name.)
3. **Real structural gap**: the compose subsystem has NO per-commit image concept at all. Legacy
   path resolves a per-commit image via `SimulatorVersion.git_commit_hash`; compose uses ONE
   static image tag (`settings.compose_ray_image_tag`) for the whole deployment. Item 35
   specifically dispatches against a PINNED commit (`8d50ff0`) — if `/compose/v1/*` can't express
   "run against commit X" today, that's real required work, not just wiring redirection.
4. ParCa is hardcoded as a non-optional first DAG node — needs to become optional/pluggable for a
   generic composite that may not have a ParCa-equivalent prep step.

**Confirmed, rigorously**: the checkpoint-handoff mechanism (`initial_carry_state_path`/
`initial_generation_index`/`daughter_state_out_path`) is a real PATTERN, not vEcoli-only — no
framework-level special-casing anywhere in `composite_spec`/`composite_generator`; any composite
author could adopt the identical 3-parameter shape. Real demonstrated proof: `KO_batch_baseline`
shares the SAME underlying Step class but hasn't opted its OWN composite parameters into exposing
the checkpoint triple yet — showing the mechanism and a composite's opt-in are cleanly separable.

**One real open design question surfaced**: there's no framework-level way to introspect "which of
a composite's declared params are its seed/generation/checkpoint knobs" — would need either (a) a
documented NAMING CONVENTION (chain-dispatchable composites declare params under fixed names, i.e.
what `batch_baseline` already does organically) or (b) new metadata support in
`composite_generator` itself, which lives in a DIFFERENT repo (`viva-superpowers`/
`pbg_superpowers`), out of viva-api's own scope. Leaning (a) for a non-invasive fix; (b) is a
legitimate longer-term refinement, not this task's scope.

**Research stream 3/3 (domain-agnostic assessment) — synthesis complete, all 3 streams in.** Major
finding: this exact topology (1 shared prep → N branches → each internally sequential) is already
implemented TWICE, independently, in this ecosystem — Nextflow's real
`vEcoli-private/runscripts/nextflow/sim.nf`/`template.nf`, AND viva-api's own hand-rolled chain,
whose own docstring admits it was built "mirroring vEcoli-private's own Nextflow task granularity."
Two independent convergent implementations = real evidence of a genuine domain-agnostic pattern,
not assertion. `ParameterScan`/`ProcessEnsemble` confirmed NOT usable (no sequential-stage concept
/ not even parallelized yet; interface-merging mixin, not fan-out). **Correction to an earlier
claim in the design doc**: `CompositeSpec.requires` is NOT an unused seam — it has a real, closed,
actively used meaning (build-time registration check, 3 real composites use it) — wrong extension
point for dispatch metadata; a NEW small `CompositeSpec.dispatch` sibling field is the right shape
instead (declares which of a composite's own params play instance-count/stage-count/resume-handoff
roles).

**A real fork surfaced, not resolved unilaterally**: Path A (generalize the hand-rolled Batch
`dependsOn` chain into `/compose/v1/*` — the 4 gaps above, well-scoped, lower unknown) vs. Path B
(route through viva-api's ALREADY-LIVE second backend, Nextflow-on-Batch, currently only serving
vEcoli-private — architecturally purer, zero new dispatch code, but v2ecoli/sms-ecoli composite
compatibility with `nextflow.py`'s renderer is completely unverified, its own unscoped
investigation). **Recommendation given to Alex**: Path A now (smaller, well-scoped, bounded
unknowns), Path B explicitly recorded as a real future direction once Path A is live — not
dropped.

**DEFERRED TO A FUTURE SESSION, 2026-08-14, explicitly — not abandoned.** Alex's own framing when
checkpointing: the Path A implementation should read as "a different session" — that session's
remaining focus was closing out item 35's pilot itself, not starting the compose-migration build.
Nothing dispatched, nothing implemented as of that checkpoint. When this gets picked back up:
re-read the design doc fresh (don't trust a stale summary of it), confirm the pilot precedent it
grew out of is still accurate, and get Alex's explicit go on Path A specifically before spending
implementation effort.

---

### Part 2 — Full technical design (reproduced in full from `viva-api/docs/DESIGN-process-bigraph-native-dispatch.md`)

#### Summary

`viva_api/simulation/simulation_service_ray.py:964-971` (`submit_ecoli_simulation_job`) decides
which AWS Batch dispatch architecture to use — a single multi-node Ray job, or a full per-seed
chain-dispatch campaign — using two incidental scalars pulled off the raw request:

```python
composite = getattr(config, "composite", None)          # a routing-string enum, NOT a
                                                          # process_bigraph.Composite object
n_generations = int(config.generations or 1)
if composite is None and n_generations > 1:
    return await self.submit_chain_dispatch_job(...)
```

The composite this dispatches — `batch_baseline` (`sms-ecoli/v2ecoli/composites/batch_baseline.py`)
— already declares `n_seeds` and `n_generations` as real, typed parameters in its own
`@composite_generator(parameters={...})` schema. The routing decision never consults that
declaration. It re-derives the same N-seeds × G-generations shape by hand
(`submit_chain_dispatch_job`'s `for seed in range(n_seeds): for generation_index in
range(n_generations):` loop), in AWS Batch's vocabulary, instead of reading it off the composite's
own vocabulary.

**Verdict**: the routing conditional is inconsistent with process-bigraph's own design philosophy —
not because the framework has no principled representation for multi-instance/multi-generation
execution (it has one, at multiple levels), but because the *decision of which dispatch shape to
use* is made one layer above where any of that representation lives.

**Scope boundary — this is NOT a re-litigation of item 46.** Backlog item 46
("v2-per-cell-wall-time-reduction") already investigated a related-sounding but distinct question —
how per-task *compute* should be isolated/allocated at the infrastructure level (CPU capping,
container bin-packing, Spot contention) — and concluded "process-bigraph confirmed to have no
opinion on this layer... 'process-bigraph-native' isn't a coherent category for the fix, by design
not gap." That finding stands and this proposal does not touch it. Item 46 is about *how compute is
physically executed*; this proposal is about *how the decision of which dispatch shape to invoke is
made*. Bigraph theory has nothing to say about CPU cgroups; it has a great deal to say about how a
composite's own declared structure should drive what happens to it.

#### Theoretical grounding

Primary source: Agmon, E. & Spangler, R.K., **"Process Bigraphs and the Architecture of
Compositional Systems Biology,"** arXiv:2512.23754 (main text + formal supplement). Earlier
foundational work by the same author: "Foundations of a Compositional Systems Biology,"
arXiv:2408.00942; Agmon et al., *Bioinformatics* 38(7):1972-1979 (2022, Vivarium 1.0). Directly
relevant applied precedent cited by the 2026 paper: Skalnik et al., *PLOS Comp. Biol.*
19(6):e1011232 (2023) — E. coli colony division modeled via the same structural-update idiom this
proposal leans on.

A **bigraph** (Milner) is two graph structures sharing nodes: a **place graph** (hierarchical
containment) and a **link graph** (connectivity). Process Bigraph keeps the place graph and
replaces the link graph with a **process graph** — processes are first-class nodes wired to typed
stores via ports. The formal object (supplement §3.6) is `B = (Σ, x, R_T, R_L)`: a typed schema
tree, a concrete state tree, a type registry, and a link registry. A **Composite** (§3.7) packages
an entire internal process-bigraph behind an external interface — composites nest recursively and
are substitutable for atomic processes.

The directly relevant mechanism is **structural deltas** (supplement §3.5.2, Table S1). Alongside
ordinary value updates, the framework defines `insert`/`delete`/`move`/`rewrite`/`rewire`
operations that modify the *shape* of the state tree itself, expressed as typed deltas through the
same update semantics as numeric updates — the paper's own example is cell division. This is not
abstract: `process_bigraph/processes/growth_division.py`'s `Divide` Step (installed in this
ecosystem's own vendored `process_bigraph`, canonical `vivarium-collective/process-bigraph`
v1.8.2) returns `{'environment': {'_remove': [mother], '_add': daughters}}` on trigger — exactly
this vocabulary. The same package ships `ProcessEnsemble` (`composite.py:913`, a container
exposing a combined interface over multiple sub-processes) and `ParameterScan`
(`processes/parameter_scan.py`, builds N independent sibling sub-composites from a parameter grid
inside one `Composite`), plus a `parallel_steps`/`parallel_processes`/`parallel_workers` config
surface on `Composite` itself. Multi-instance execution is supported at the library level, not a
gap application code has to invent around.

This ecosystem's own model layer already answers the question twice, deliberately:
`v2ecoli/v2ecoli/steps/division.py`'s `Division` Step is the production, bigraph-native answer —
`{'agents': {'_remove': [self.agent_id], '_add': [(d1_id, d1_cell), (d2_id, d2_cell)]}}`, matching
the paper's own worked example almost exactly. Separately,
`v2ecoli/library/vivarium_ecoli_engine.py` implements an alternate, explicitly *non*-structural
single-lineage generation driver, with its own docstring stating plainly this is a deliberate,
documented tradeoff to avoid a known `_add`-path bug — not a claim that bigraph theory lacks the
representation. Both still run inside process-bigraph's own object model.

#### Evidence this is fixable now, not just theoretically ideal

`CompositeSpec` is process-bigraph's *own* dataclass, not an ecosystem convention layered on top
(`process_bigraph/composite_spec.py:240`), with a single shared registry
(`_REGISTRY`/`register`/`get`, lines 431-447) that every `@composite_generator`/`@composite_spec`
decorator populates and `discover_specs()` walks. `CompositeSpec.parameters` already backs
`_merged_params`'s override-wins-over-declared-default resolution (lines 327-334) — the canonical
implementation of exactly the precedence semantics this ecosystem has repeatedly wanted.

Three independent call sites in this ecosystem already resolve composite identity/parameters
through this one shared registry:

1. **`viva_api/compose/run_pbg.py:169-179`** — same repo as the flagged code, one directory over.
   `_resolve_document()` calls `get_spec(composite_id)`, falling back to `discover_specs()` then
   retrying. Its own docstring states the intended scope directly: *"This is what lets a
   model-specific dispatcher (e.g. viva-api's vEcoli ensemble endpoint) submit a config-driven run
   through the exact same execution mechanism a hand-authored `.pbg` document uses"* — naming the
   very subsystem containing the flagged conditional as the intended consumer it doesn't yet have.
2. **`vivarium_workbench/lib/composite_resolve.py`** — `classify_run_kind()` (lines 106-143) walks
   a composite's resolved state tree and classifies it `temporal`/`workflow`/`unknown` purely from
   each node's own `_type` field (`"process"` vs `"step"` — process-bigraph's own native scheduling
   discriminant, not an ecosystem-invented flag). Wired end-to-end into `/api/composite-resolve`
   and the Configure-run form's field generation (`static/configure-run.js`).
3. **`vivarium_workbench/lib/pbg_export.py`** — the workbench's own Composites-tab remote-run
   export path, per `run_pbg.py`'s own comment, resolves through the identical registry.

Only the flagged routing decision in `simulation_service_ray.py` still reasons about request-shape
proxies instead of resolving the named composite through this already-shared mechanism.

`CompositeSpec.requires` (line 250, threaded through `resolve_composite()`'s payload and
`CompositeSpec.from_file`'s `requires:` YAML key) — **note the correction in Part 1 above**: this
is NOT an unused seam, it already has a real, closed, actively-used meaning (build-time
registration check) — not the right extension point for dispatch metadata.

#### Scope of the problem — full inventory

The pattern is concentrated in viva-api, keying off two `SimulationConfig` fields: `composite`
(a `Literal["v2ecoli","vecoli"]` routing enum — despite the name, not a `process_bigraph.Composite`
object) and `generations` (a plain int):

| # | Location | What it decides |
|---|---|---|
| 1 | `simulation_service_ray.py:964-971` | Single MNP job vs. full chain-dispatch campaign (the flagged instance) |
| 2 | `simulation_service_ray.py:516-599` (`_sim_command`) | Among **three** different scripts (`run_comparison_ensemble.py` / generic `run_pbg.py` against `batch_baseline` / `run_phase0_xarray_ensemble.py`) |
| 3 | `simulation_service_ray.py:1013` | Whether to stage a runner S3 URI at all |
| 4 | `simulation_service_ray.py:731` | A weaker instance — `isinstance(modules, str)`, a type-check rather than composite metadata |
| 5 | `common/handlers/simulations.py:508-515` | Origin point: where the `composite` routing flag first enters `SimulationConfig` as an "extra" passthrough key |
| 6 | `config.py:326-349` (`compute_backend_for_repo`) + `common/handlers/simulations.py:440,1633-1650` | SLURM vs. K8s/Nextflow vs. Ray, by string-matching `simulator.git_repo_url` — a more defensible variant (repo-identity routing) but still "architecture decided by something outside composite structure" |

One further instance one layer down, inside the model repo itself:
**`v2ecoli/v2ecoli/workflow/run.py:58-121`** (`_resolve_parallel`/`run_workflow`) — Ray
worker-per-seed fan-out vs. single-process meta-composite, decided by an incidental `mode` string
plus `has_variants = bool(config.get("variants"))`; the docstring admits variant sweeps are
excluded from the parallel path "for now" — an acknowledged carve-out, not a structural
requirement.

**Vivarium-workbench does not have this problem** — its local-vs-remote execution decision is
already centralized in one resolver (`lib/run_core.py:18-21` `run_target_for` +
`lib/remote_pinned.py:63-100` `resolve_run_target`), built specifically (per its own docstring,
citing item 18) to stop different call sites from resolving local/remote differently. Two
workbench call sites already ARE composite/study-structure-derived and are the shape to generalize
toward: `lib/study_runs.py:679-701` (routes on the variant's own declared `kind:` in `study.yaml`)
and `lib/composite_runs.py:132-158` `run_with_division` (stops on a real runtime signal the
composite itself produces, not a config flag).

**`pbg-template` cannot have this problem** — it ships no execution/dispatch concept at all
(confirmed by full-repo search); execution is entirely downstream of the scaffold it provides.

#### Proposed design directions (original, narrower scope — see MAJOR SCOPE EXPANSION below for the current, larger scope)

**Direction A — schema-declared topology (recommended, minimal diff, extends an existing
same-repo mechanism).** Replace the flag-based routing condition in
`simulation_service_ray.py:964-971` with one that resolves the named composite via
`process_bigraph.composite_spec.get()`/`discover_specs()` — the exact mechanism `run_pbg.py`
already imports and uses two files over in the same package — and reads dispatch-relevant shape
off its own declared `parameters` (`batch_baseline` already declares `n_seeds`/`n_generations`)
rather than off `SimulationConfig`'s incidental fields.

**Direction B — structural-delta-driven generation progression (theoretically purer, deliberately
NOT proposed for immediate implementation).** Re-express generation-to-generation progression as
an actual process-bigraph structural delta (`_add`/`_remove`, matching
`v2ecoli/steps/division.py`'s own pattern) rather than a hand-built AWS Batch `dependsOn` chain.
Closer to the framework's own idiom, but a substantially larger change, not concretely scoped, its
value over Direction A not established. Recorded as a real future direction, not dropped.

**Proposed revision plan (Direction A, original narrow scope)**:
1. Resolve the target composite via `process_bigraph.composite_spec.get(composite_id)` (mirroring
   `run_pbg.py:169-179` exactly) instead of reading `composite`/`n_generations` off
   `SimulationConfig` directly.
2. Read the resolved `CompositeSpec.parameters` for `n_seeds`/`n_generations` to decide
   chain-dispatch vs. single-job. Explicit request-level overrides continue to win over the
   composite's own declared defaults — same precedence `CompositeSpec._merged_params` already
   implements.
3. Keep `submit_chain_dispatch_job`'s actual per-seed/per-generation AWS Batch submission mechanism
   unchanged — only the routing decision changes.
4. Real regression tests against the live registry, no mocking `composite_spec.get()` itself.
5. Bundle fix + tests + version bump in one PR, following viva-api's release protocol.
6. Deploy on the normal tag → release → build → deploy cadence, once merged.

#### Revision, 2026-08-14 — architectural correction after verifying against real planning docs

An earlier draft additionally suggested adding a generic `overrides: dict` passthrough directly to
`POST /api/v1/simulations`. **Withdrawn** — verified directly against `viva-api/docs/PRE-DEMO-PLAN.md`
and `PRE-DEMO-MASTER-PLAN.md` plus the current source tree, this would work against an
already-made, already-partially-built architectural decision (see Part 1 above for the full
evidence: `/compose/v1/*` is the already-designated, already-partially-built home for "faithful,
any-composite, any-domain access," not something Direction A needed to build; `/api/v1/simulations`
is deliberately kept as the vEcoli-ensemble specialization). Direction A's core fix (read the
routing decision off the composite's own declared parameters) was unaffected by this correction,
and better justified by it.

#### MAJOR SCOPE EXPANSION, 2026-08-14 — the current, operative scope

Alex made the deferred question ("should item 35 eventually run through `/compose/v1/*`?") the new
mandate: `/api/v1/simulations` is explicitly back-compat only; the canonical fully-remote dispatch
**must** run through `/compose/v1/*`, non-invasively.

**Is this pattern genuinely domain-agnostic? Yes** — the exact topology needed (one shared prep
step → N parallel branches → each branch internally sequential) is already implemented TWICE,
independently, in this ecosystem: Nextflow's `vEcoli-private/runscripts/nextflow/sim.nf`/
`template.nf` (a real, shipped, general-purpose workflow engine's native idiom, not invented for
whole-cell modeling), and viva-api's own hand-rolled AWS Batch `dependsOn` chain
(`submit_chain_dispatch_job`), whose own docstring admits it was built *"mirroring vEcoli-private's
own Nextflow task granularity."* Two independent convergent implementations of the same shape is
real evidence the shape is the pattern, not an artifact of one implementation's choices. Further
corroborated by domain analogies with real precedent (MCMC multi-chain sampling, HPC
checkpoint/restart across ensemble members, genomics cohort pipelines) and by `batch_baseline`
itself already generalizing past pure biology internally (`variants` as a generic sweep-grid axis).

`ParameterScan` and `ProcessEnsemble` (process-bigraph's own library) checked directly and are
**not** usable building blocks: `ParameterScan` has no internal-sequential-stages concept and isn't
even parallelized yet (a `# TODO` in the library itself); `ProcessEnsemble` is an interface-merging
mixin, not a fan-out mechanism. `CompositeSpec.requires` — corrected above — has a real, closed,
actively-used meaning (build-time "what must be registered" declaration, 3 real composites use it)
— not the right extension point for dispatch metadata.

**A real fork in the design space — two legitimate paths, not one:**

**Path A — generalize the hand-rolled AWS Batch chain into the compose subsystem.** What's already
reusable as-is, zero new code: `_submit_mnp` (with `depends_on`), `_ensure_mnp_job_def`,
`_SubmitJobPacer`, the retry client/strategy, the campaign-DB schema (`HpcRun.chain_n_generations`/
`chain_final_job_ids` — confirmed generic, plain int/JSONB list, no vEcoli shape), and critically
`run_pbg.py --composite-id <id> --overrides <json>` (the generic runner
`_seed_generation_command` already targets) — `ComposeSimulationServiceRay` already holds a real
`SimulationServiceRay` instance (`self._ray`) and can call `self._ray._submit_mnp(...,
depends_on=[...])` today, mechanically, with no new imports. What's genuinely gap, four concrete
places:
1. Where `n_seeds`/`n_generations`/composite identity are sourced from — viva-api's own ORM + a
   hardcoded `V2ECOLI_BATCH_BASELINE_COMPOSITE_ID` constant, instead of the resolved composite's
   own declared `parameters`.
2. The override-key vocabulary is hardcoded as literal strings, even though these ARE the
   composite's own real parameter names — needs to read them off the resolved spec.
3. **Real structural gap**: the compose subsystem has no per-commit image concept at all. Legacy
   resolves a per-commit image via `SimulatorVersion.git_commit_hash`; compose uses one static
   image tag for the whole deployment. Item 35 dispatches against a pinned commit — real work
   needed, not just redirection.
4. ParCa is hardcoded as a non-optional first DAG node — needs to become optional/pluggable.

Additionally needed either way: new seed/generation-equivalent fields end-to-end on
`ComposeSimulationRequest`/the router, a place to persist per-instance job ids (extend
`ComposeHpcRun` or reuse the existing generic `HpcRun` table), and a fan-in poller
(`JobScheduler.update_chain_campaigns` is hard-typed to concrete `SimulationServiceRay` today —
needs generalizing or a compose-side sibling on `ComposeJobMonitor`, which currently has zero
chain-related methods).

For a composite to self-describe which of its own parameters play the instance-count/stage-count/
resume-handoff roles (no framework-level introspection for this exists today — `batch_baseline`'s
`n_seeds`/`n_generations` naming is organic convention, not declared metadata), the clean small
addition is a new `CompositeSpec` sibling field — NOT a `requires` overload, a plain new field
following the exact extension pattern every other field there already uses:

```yaml
dispatch:
  topology: chain
  instances: n_seeds
  stages: n_generations
  resume: {stage_index_param: initial_generation_index,
           carry_state_in_param: initial_carry_state_path,
           carry_state_out_param: daughter_state_out_path}
```

**Path B — route through the already-live Nextflow-on-Batch backend.** viva-api already operates a
full, working, second backend (`ComputeBackend.BATCH = "batch"  # AWS Batch via Nextflow`,
`simulation_service_k8s.py`'s K8s-Job-runs-Nextflow-head pipeline) — currently serving
vEcoli-private only. process-bigraph's own `nextflow.py` (438 lines, compiles a Composite's step
graph into a real Nextflow DSL2 document) plus `plumbing.py`'s `Mix`/`Collect`/`Combine`/`GroupBy`/
`Join` dataflow Steps (explicitly designed so composite documents can express Nextflow-channel-style
dataflow "while still running natively in the process-bigraph engine") together mean a composite
COULD declare its N×G shape declaratively and have Nextflow's own native semantics execute it —
zero bespoke chain-dispatch code needed on viva-api's side at all. Architecturally the purest fit.
Real unknown: whether v2ecoli/sms-ecoli's composites render correctly through `nextflow.py` today
is unverified — this backend currently only serves vEcoli-private, and connecting it to the v2ecoli
model family is its own, unscoped investigation.

**Recommendation: Path A now, Path B flagged as a real future direction, not dropped.** Path A is
the smaller, already-well-scoped, lower-unknown option — the four gaps above are concrete and each
individually bounded, all sitting on top of already-shared, already-proven primitives. Path B is
architecturally more elegant but carries a real, currently-unscoped unknown that would need its own
investigation phase before any time estimate could be trusted.

---

### Part 3 — Comprehensive review against PR #805, completed 2026-08-14

**The question posed** (see the UPDATE note above): does PR #805's comparison-harness dispatch
shape count as evidence FOR building this item's Path A now, or is it a separate effort that only
happens to share a subsystem?

**PR #805 verified firsthand** (`gh pr view 805`/`gh pr diff 805 --repo
vivarium-collective/vivarium-workbench`, not the pasted-URL summary it was first logged from):
OPEN, Jim Schaff, adds one new file, `docs/dual-engine-comparison.md` (274 lines, docs only, no
code). Real content, not just the PR body blurb: rejects fork-as-input (§2, three reasons — the
reference wouldn't run under its own lock, doesn't generalize past import-compatible engines, and
is asymmetric where the science is symmetric); narrows REFACTOR-PLAN §2A.5's Q1 from
per-workspace to **per-run** ("a run pins exactly one environment coordinate, but an
investigation's member runs may pin different coordinates" — verified consistent with §2A.2's own
run-as-binding shape, `{study@ver, composite_ref, env_id, params, outputs_uri, provenance}`, and
with §2A.5's actual Q1 text, "the runtime always resolves exactly one environment" — the narrowing
preserves that invariant per-node, it doesn't relax it); a 4-node DAG (candidate-sim env A ·
reference-sim env B · optional transfer node · compare+report); one new data contract (§4, a
deliberately tiny per-engine "comparison-metrics v1" artifact); W1-W6 workstreams table with **W4
explicitly viva-api-owned** (AWS dispatch: register `vEcoli-private` as a second simulator source,
two ordinary Batch jobs one per engine, one dependent compare job) and an Appendix A hand-off
prompt asking viva-api four direct questions.

One real, unresolved cross-design overlap surfaced by same-day activity (`gh api
repos/.../issues/805/comments`, `cplong90`, MEMBER, 2026-08-14T16:01:19Z — the same timestamp
PR #805's own `updatedAt` shows, confirming this is genuinely new since the plan above was first
written): §4's normalized-metrics artifact may want to become a projection of a separately real
`Operand` type from `vivarium-collective/v2ecoli#486` (verified real and OPEN via `gh pr view 486
--repo vivarium-collective/v2ecoli` — "a report card grades two operands... what determines how an
operand is resolved is the *path* it arrived by... not whether it is measurement or simulation").
This is an artifact-schema question, orthogonal to viva-api's dispatch mechanics below — it doesn't
change this section's conclusion, but it means PR #805 itself is not yet settled end-to-end, which
matters for sequencing (see the close-out note at the bottom of this section).

**Answer: partial yes — real, concrete evidence FOR building Path A's job-dependency
generalization now, but PR #805's W4 needs one capability beyond what Path A currently scopes.**
Reasoning, grounded in current source (re-verified today against `viva-api` directly, not assumed
from this file's own possibly-stale line numbers):

1. **The low-level primitive PR #805's W4 needs already exists, and it's exactly what Path A
   already plans to generalize.** `_submit_mnp(..., depends_on=[...])`
   (`viva-api/viva_api/simulation/simulation_service_ray.py:337`) is the same job-dependency
   primitive `ComposeSimulationServiceRay` already calls today
   (`viva-api/viva_api/compose/simulation_service_ray.py:162`, via `self._ray._submit_mnp(...)` —
   `self._ray` is a real, live `SimulationServiceRay` instance, confirmed at line 65 of that file).
   PR #805 §5.1 point 3 ("one dependent compare job... Batch job dependencies") and Appendix A Q3
   ("Does viva-api's Batch integration expose job dependencies... or should the workbench
   poll-and-dispatch the compare job itself?") are asking for exactly this — and the honest answer
   is **yes, today, in production**: this is the identical mechanism item 35's real chain-dispatch
   pilot used for all 20 real `chain-seed{N}-gen{N}` AWS Batch jobs (see
   `.todo/backlog/35.md`, Status: DONE). That's real, non-speculative, second-consumer pressure to
   do the Path A generalization (making `/compose/v1/*` express dependency-chained dispatch
   generically) rather than leave it a hand-rolled, `/api/v1/simulations`-only mechanism — a second
   real caller is exactly the kind of evidence that justifies generalizing a pattern now instead of
   deferring it.

2. **But PR #805's actual topology is heterogeneous, not the homogeneous N-seed/N-generation shape
   Path A's 4 gaps (Part 2 above) are scoped around.** Those 4 gaps are entirely about ONE
   composite (`batch_baseline`) dispatched with different seed/generation params read off ITS OWN
   declared `CompositeSpec.parameters` — same image, same runner, same repo, N homogeneous
   branches. PR #805's DAG is 2 branches of TWO DIFFERENT ENGINES (sms-ecoli's own process-bigraph
   composite vs. `vEcoli-private`'s native runscripts — different repo, different image, no shared
   composite) converging on 1 compare node. It can never be expressed as "resolve composite X's own
   n_seeds/n_generations," because the reference side isn't a process-bigraph composite at all.

3. **The real gap PR #805 introduces that Path A does not currently scope**: Appendix A Q2 asks
   viva-api directly for a "generic containerized job: image + command + env + outputs-to-S3 — with
   no pbg assumptions" for the vEcoli-private side. That's a genuinely different capability from any
   of Path A's 4 gaps (which all assume a resolvable `CompositeSpec`) — closer to "run this exact
   command in this exact image" than to "read n_seeds/n_generations off a composite's own declared
   parameters." Recommend folding this in as a real 5th Path A gap (**gap 5 — generic non-pbg job
   dispatch**) rather than treating it as a reason to fork into separate, unrelated wiring: it still
   dispatches through the exact same `_submit_mnp`/`_ensure_mnp_job_def`/retry-client primitives
   Path A already reuses; it just skips the `CompositeSpec`-resolution step for the one side that
   has no composite to resolve.

**Concrete recommendation for next session, when Path A is picked up**: scope Path A's
implementation to cover BOTH real consumers from the start — item 1's homogeneous
N-seed/N-generation baseline chain-dispatch (gaps 1-4, already scoped) AND a minimal generic
containerized-job dispatch primitive (gap 5, motivated directly by PR #805's W4) — rather than
building gaps 1-4 alone and rediscovering gap 5 mid-way through a future W4 session. Both
consumers share the same underlying `_submit_mnp(depends_on=[...])` + per-backend-registry
machinery already verified live in `ComposeSimulationServiceRay`; adding gap 5 alongside gaps 1-4
is incremental scope, not a second design effort. This directly and concretely answers PR #805's
own Appendix A Q3 — worth relaying back to Jim Schaff (a PR comment, or folded into the eventual
Path A design writeup) that viva-api's job-dependency wiring already exists in production and this
item is where its generic (`/compose/v1/*`) exposure will land.

**Not this item's job to resolve**: PR #805 §7.1 (ratifying the REFACTOR-PLAN §2A.5 Q1 narrowing),
§7.2 (investigation home), §7.4 (compare-node environment), or the cplong90/v2ecoli#486
Operand-vs-metrics-artifact schema question — all genuinely workbench/science-side decisions,
orthogonal to viva-api's dispatch mechanics above. One sequencing note worth flagging: PR #805
itself leans on PR #804 (the REFACTOR-PLAN §0A status reconciliation) for its "ratify against
REFACTOR-PLAN" ask, and PR #804 is **also still OPEN, not merged** (confirmed via `gh pr view 804`)
— §7.1's ask is to amend a document whose own reconciliation PR hasn't landed yet. Not blocking for
viva-api's W4 specifically (which only needs the job-dependency answer in point 1 above), but worth
knowing so nobody assumes REFACTOR-PLAN's Q1 is fully settled before PR #804 lands.

---

## Status: PENDING — re-activated, sequenced, not yet started

**Description**: Fully scoped (three independent research streams, each verified against real
source), a recommendation given to Alex (Path A over Path B), and explicitly deferred to a future
session as of 2026-08-14 — not started. No branch, no code, no PR. **The comprehensive review
against PR #805 that gated starting implementation is now done (Part 3 above, 2026-08-14)** — Path
A should proceed scoped to 5 gaps (the original 4 plus a generic non-pbg job-dispatch primitive),
not 4. Still not started — no branch, no code, no PR.

**GOAL EXPANDED, 2026-08-16**: Alex's explicit `/goal` re-activates this item now, converging it
with item 52 and item 1 (session renamed `backlog:1-50-52`) — "using backlog item 1 as the targeted
goal/use-case to verify items 50 & 52," merged into one effort where feasible. This is the explicit
go on Path A (5-gap scope) the note above was waiting for. Sequenced AFTER item 52's own bugs (55,
56, 57, **and 58**) are fixed and validated via a small pilot — no point building a new dispatch
path on top of an unvalidated composite/state layer. Full plan: memory
`item1-real-1000x10-dispatch-live.md`. Not yet started as of this note — still no branch, no code,
no PR.

**Re-verified against live source, 2026-08-16 (agent `af848add33615fef5`, read-only pass, no code
changes)**: plan confirmed sound, zero substantive drift. Routing conditional, all 5 gaps, and every
reusable primitive (`_submit_mnp`, `_ensure_mnp_job_def`, the campaign DB schema,
`ComposeSimulationServiceRay.self._ray`) all checked against current source and still accurate —
the two most load-bearing call sites (`self._ray._submit_mnp(...)`) confirmed "exact match, zero
drift." Two trivial corrections applied above (gap 2's example key `base_seed`→`seed`, item 52's
blocker list now includes item 58). PR #805/#804 both still OPEN, unchanged. Sequencing conclusion
independently re-confirmed correct: item 52 is still genuinely unvalidated (item 58 is why) at the
time of this check, so Path A's precondition remains correctly unmet. **When item 52 actually
validates, Path A can start immediately — no further re-verification needed**, just implement.

---

### Part 4 — HOLISTIC SCOPE REVISION, 2026-08-16, after item 60 — a real 6th gap, not a footnote

**Item 52 validated the same day this section was written** (real per-generation wall time
62-67s, ~5x under target — see `52.md`). Its own validation pilot (sim 156) then hit item 60: the
auto-triggered ANALYSIS stage failed on every multi-generation run
(`run_standalone_analysis.py: error: unrecognized arguments: --n-generations 2`). Alex's explicit
instruction on discovering this: *"plan for item 50 must holistically change, given what item 60
revealed"* — not treat item 60 as an unrelated bug fixed and forgotten.

**Why item 60 is architecturally load-bearing for this item, not just an adjacent bug**: read
`simulation_service_ray.py`'s own docstring for `_build_analysis_command` (the function item 60's
root-cause investigation traced the bug to) directly:

> "WHY A THIRD DAG NODE, not the composite's own inline flush... The whole-sweep analysis is
> therefore a GATHER node, and the DAG edge that expresses 'after every child succeeded' is the SAME
> Batch `dependsOn` the ParCa→sim edge already uses."

This is the code's own admission that the real DAG this item's Path A needs to generalize is
**ParCa → N×G simulation chain → analysis GATHER node — three stages, not two.** Gaps 1-4 (as
originally scoped) cover sourcing simulation-dispatch shape off the composite's own declared
parameters. They say nothing about the analysis GATHER node, which:
- uses the exact same `_submit_mnp`(`depends_on=[...]`) primitive gaps 1-4 already plan to
  generalize (confirmed directly in the docstring above — not a different mechanism, the SAME one),
- is constructed with an equally hardcoded, sms-specific command string
  (`_build_analysis_command`, `simulation_service_ray.py:700-759`) — the caller-side half of exactly
  the bug item 60 fixed on the callee side,
- was the reason item 1's own stated deliverable (real `cd1_*`/`ptools_*` artifacts, generated
  reproducibly through the fully remote canonical workflow) was still blocked even after items
  55-58 and 52 were all fixed/validated.

**A real, durable primitive item 60 already produced, worth reusing here directly**: the fix added
`resolve_modules()`/reused `batch_baseline_runner.build_analysis_options()` in v2ecoli's
`run_standalone_analysis.py` — resolving which analyses apply from `n_seeds`/`n_generations`
*read off the actual composite's shape*, not from a caller-hardcoded assumption. This is exactly
this item's own founding principle (dispatch decisions driven by the composite's own declared
structure, not incidental request fields) already proven out, independently, one layer down, on the
analysis side. Gap 6 below is the caller-side (viva-api) completion of the same idea — it should
call through to this already-fixed, already-tested resolution, not reinvent it.

#### Gap 6 (NEW) — generalize the analysis/GATHER DAG node onto `/compose/v1/*`, same as gaps 1-4 do for simulation dispatch

Concretely, the compose subsystem needs an analysis-GATHER equivalent of what gaps 1-4 already plan
for the simulation chain:
1. A composite's own declared parameters (extending the same `CompositeSpec.dispatch` sibling field
   sketched in Part 2 — `topology`/`instances`/`stages`/`resume`) should gain an `analysis` sub-key
   (e.g. which modules/scale-keyword apply by default, whether a GATHER analysis node fires at all)
   — read at dispatch time, not hardcoded per-caller the way `modules="applicable"` is hardcoded into
   `simulation_service_ray.py` today.
2. `ComposeSimulationServiceRay` needs its own `_submit_analysis_job`-equivalent, built the same way
   gaps 1-4's simulation-chain method is: call `self._ray._submit_mnp(..., depends_on=[...])`
   directly (already mechanically reachable, zero new primitive), constructing the
   `run_standalone_analysis.py` invocation from the composite's *resolved* parameters instead of a
   caller-hardcoded string.
3. The analysis result needs the same fan-in/discoverability treatment gaps 1-4 already plan for
   campaign tracking (`analyses` table entry, `GET /analyses/{id}/status`) — plus, surfaced by item
   60's own investigation, real DB-status reconciliation against terminal AWS Batch state (today
   `analyses` rows can silently go stale — item 60's pilot showed a job dead for 26+ minutes while
   its DB record still said `"running"`). Worth fixing as part of gap 6 rather than leaving it a
   second, separately-discovered gap on the new path too.

**Sequencing**: gap 6 is additive to gaps 1-5, not a blocker for starting them — the simulation-chain
generalization (gaps 1-4) and the generic non-pbg primitive (gap 5) don't depend on gap 6, and gap 6
depends on nothing gaps 1-4 haven't already established (the same `_submit_mnp`/campaign-DB
machinery). All 6 can be implemented in one pass; gap 6 doesn't need its own separate future session.

**What this does NOT change**: the recommendation (Path A over Path B), the core mechanism inventory
(gaps 1-5 exactly as scoped in Parts 2-3), or the "don't collapse `/api/v1/simulations` and
`/compose/v1/*`" architectural boundary from Part 1. It closes a real scope gap the original 5-gap
enumeration didn't know to look for until item 60 surfaced it.

---

### Part 5 — Gap 6 concrete design, folding in viva-api PR #239, 2026-08-16

**Alex's explicit instruction**: viva-api PR #239 (`feat(item 38 track B): OOM-retry-escalation for
the analysis DAG node`, OPEN, unmerged, 164 lines on `simulation_service_ray.py`) should be **folded
into Gap 6**, not landed separately first. Read in full (`gh pr diff 239`, not the PR body summary)
before designing this section — real logic, verified against current source, below.

**What PR #239 actually does** (legacy `/api/v1/simulations` path only):
- `analysis_memory_mib_for(config)` reads a new `AnalysisOptions.memory_gb` field.
- `_ensure_mnp_job_def` extended to cache a distinct job-def revision per `(commit, memory_mib)`.
- `resubmit_analysis()` resubmits an OOM'd analysis at escalated memory, reusing the same
  `analysis_name` (same S3 output prefix), via `self._ray._submit_mnp(..., depends_on=None)`.
- `JobScheduler.update_analysis_retries()`: a genuinely NEW poller — **nothing previously polled a
  submitted analysis job to terminal state at all**; status only ever changed on-demand via
  `GET /analyses/{id}/status`. Detects OOM (`exit_code == "137"`), escalates memory `(attempt+1)×`
  baseline, caps at 3 attempts, else marks FAILED. New `Analysis.attempt` column + migration.
- **A real, separately-valuable, backend-general fix**: `SimulationServiceRay.get_job_status()` was
  silently never reading `exit_code`/`error_message` for MNP jobs at all (`exit_code=None` always) —
  Batch MNP jobs never populate the top-level `container`, only `attempts[-1].container`. This isn't
  analysis-specific; it's a real correctness bug in the shared status-polling method every MNP job
  (simulation chain jobs too) goes through.

**This directly explains item 60's own DB-reconciliation finding** (an analysis stuck showing
`"running"` for 26+ minutes after really failing in ~43s) — PR #239's poller is exactly the missing
mechanism. Folding it in doesn't just add retry-escalation, it fixes the staleness bug too.

**The real design fork, resolved**: `viva_api/compose/tables_orm.py`'s `ORMComposeHpcRun` has
`job_type: ComposeJobTypeDB` with exactly two values, `SIMULATION`/`BUILD_CONTAINER` — **zero
analysis concept on the compose side at all**, and no `config`/`attempt`/`result_uri` fields to
support retry reconstruction the way legacy `analysis` already does. Forcing analysis semantics into
`ComposeHpcRun` would fight its existing shape. Decision: **a new `compose_analysis` table**,
mirroring legacy `analysis`'s real shape (`config`, `attempt`, `status`, `job_id_ext`, `result_uri`,
FK to `compose_simulation`) — reusing an already-proven shape rather than inventing a new one.

**What's genuinely shared vs. what needs its own compose-side implementation**, checked directly
against real source, not assumed:
1. `get_job_status`'s MNP-container fix — lands once in `SimulationServiceRay.get_job_status`
   (shared). `ComposeSimulationServiceRay.get_job_status` already delegates to
   `self._ray.get_job_status()` (confirmed, `compose/simulation_service_ray.py:184-190`) — the fix
   applies to compose jobs automatically, zero compose-side code change needed for this part alone.
   `_JOBSTATUS_TO_COMPOSE`'s translation currently drops `exit_code`/`error_message` on the floor
   though — needs a small additive extension to actually surface them for the retry poller to read.
2. `ComposeJobMonitor` (`compose/job_monitor.py`) **already has real, generic per-backend polling**
   (`_update_backend_jobs`, calling `service.get_job_status(hpc_run.job_id_ext)` per row) — the
   compose-side analog of `JobScheduler.update_running_jobs`. Basic status-transition polling for
   `compose_analysis` rows can extend this existing mechanism, not duplicate it.
3. The retry-ESCALATION ALGORITHM itself (OOM-detect → escalate memory → resubmit, capped at N
   attempts) is pure and data-shape-agnostic. Extract it as a small shared function both the legacy
   `JobScheduler._advance_analysis_retry` and a new compose-side equivalent call — do not duplicate
   the algorithm, only the data-fetch/resubmit plumbing differs (legacy resolves via
   `Simulation`/`Simulator`; compose resolves via `ComposeSimulation`/`ComposeSimulatorVersion`).
4. `resubmit_analysis`'s job-def/command construction (`_ensure_mnp_job_def(..., memory_mib=...)`,
   `_submit_mnp`) is already shared (`self._ray`) — compose's own analysis-submit method reuses it
   directly, same as gaps 1-4 already plan for simulation dispatch.

**Concrete Gap 6 build order** (dependency-respecting): (a) land the `get_job_status` MNP fix in
`simulation_service_ray.py` first — small, purely additive, zero design risk, benefits both paths
immediately. (b) `compose_analysis` table + migration. (c) `ComposeSimulationServiceRay`'s
analysis-submit method (composite-parameter-driven, reusing item 60's `run_standalone_analysis.py`
fix). (d) Extend `ComposeJobMonitor` with the shared retry-escalation algorithm from point 3 above.
(e) Router/model plumbing (`ComposeAnalysisOptions`-equivalent, `GET`/`POST` endpoints mirroring the
legacy analysis endpoints' shape).

**PR #239 itself**: close it unmerged (superseded), or repoint its branch at this design once (a)-(e)
land — Alex's call at that point, not pre-decided here.

## Implementation started, 2026-08-16 — build steps (a) and (b) done and tested

Worktree: `<scratchpad>/viva-api-item50`, branch `feat/item50-compose-native-dispatch`, off real
`origin/main` (`ec75e415`). Commit `ad392c61`.

- **(a) DONE, tested**: `SimulationServiceRay.get_job_status`'s MNP container/exit-code fix, folded in
  directly from PR #239 (shared/backend-general — `ComposeSimulationServiceRay.get_job_status`
  already delegates to it, confirmed by reading the real source, so this benefits both paths with
  zero compose-side change). 2 new regression tests, both passing (real `attempts[-1].container`
  fallback + top-level-container-precedence cases). `tests/simulation/test_ray_backend.py` full file:
  69 passed.
- **(b) DONE, verified compiles+imports**: `compose_analysis` schema (`ORMComposeAnalysis` +
  `ComposeAnalysisStatusDB` in `tables_orm.py`, `ComposeAnalysis` + `ComposeAnalysisStatus` in
  `models.py`) — a new table, deliberately not folded into `compose_hpcrun` (real schema gap
  confirmed by reading its actual columns: `job_type` only has SIMULATION/BUILD_CONTAINER, no
  config/attempt/result_uri). `create_all`-provisioned, no Alembic migration needed for a new table
  (confirmed against every other compose_* table's real history — only column-additions to
  already-existing tables have needed explicit migrations).
- **Full local `tests/` run hung** (known, documented risk — see memory
  `reference-viva-api-local-testcontainers-can-hang.md`) — killed, not a real failure signal. Ran the
  specific touched file directly instead (69 passed, real Postgres-backed). Full verification deferred
  to real CI once a PR opens, matching this repo's own established workaround for this exact class of
  local-environment issue.

**Remaining for Gap 6** (steps c-e from Part 5's build order): `ComposeSimulationServiceRay`
analysis-submit method (command construction reusing item 60's `run_standalone_analysis.py` fix,
`n_seeds`/`n_generations` accepted as explicit params — same shape the legacy
`_submit_analysis_job` already uses, real composite-driven resolution deferred to gaps 1-4's
`CompositeSpec.dispatch` mechanism, which doesn't exist yet); `ComposeDatabaseService` analysis CRUD
(mirroring `HPCDatabaseService`'s ABC+impl split); `ComposeJobMonitor` retry-escalation extension
(shared algorithm, not duplicated, per Part 5 point 3); router/model plumbing.

**Gaps 1-4 not yet started** — genuinely the larger, more foundational piece, and Gap 6's own
"composite-driven" analysis dispatch depends on gaps 1-4's `CompositeSpec.dispatch` mechanism to be
real rather than caller-supplied. Honest sequencing note, not previously stated this explicitly:
finishing Gap 6 fully composite-driven (not just mechanically buildable) needs gaps 1-4 done first.

---

## Gap 6 COMPLETE — all 5 pieces (a-e), built concurrently while item 60 held the critical path, 2026-08-16/17

Item 50 is decoupled/non-gating (see `1.md`'s newest section) — this ran as background, parallel work
via a fork, explicitly barred from opening a PR, while the item-60/item-1 critical path was handled
in the main thread. **Independently re-verified before trusting the fork's own report** (real commit
log, real branch-vs-origin match, a real targeted test run, real ruff check) — not taken on the
fork's word alone, same standing rule as every other claim this session.

**Real, verified state**: 6 atomic commits on `feat/item50-compose-native-dispatch`
(`93454bcc`→`54e3f300`), pushed, branch matches `origin/feat/item50-compose-native-dispatch` exactly.
Working tree clean.

- (c) Shared memory-escalation primitives (`analysis_memory_mib_for`, `_ensure_mnp_job_def(...,
  memory_mib=...)`, ported from PR #239) + new pure `viva_api/common/analysis_retry.py::decide_analysis_retry()`
  — the OOM-retry-escalation decision extracted as a shared, non-duplicated helper (Part 5 point 3,
  honored). `AnalysisDatabaseService`/`AnalysisORMExecutor` CRUD for `compose_analysis`, mirrors
  `HPCDatabaseService`'s ABC+impl split. `ComposeSimulationServiceRay.submit_analysis`/
  `resubmit_analysis`/`get_job_status_info`, reusing `self._ray`'s existing command construction —
  **correctly built as the mechanically-buildable, caller-supplied-params version** (`n_seeds`/
  `n_generations` explicit), NOT the fully composite-driven version — matches this file's own
  sequencing note above that composite-driven dispatch needs gaps 1-4's `CompositeSpec.dispatch`
  first. No scope creep into gaps 1-4.
- (d) `ComposeJobMonitor.update_analysis_retries`/`_advance_analysis_retry`, wired into the existing
  `_polling_loop`.
- (e) `POST /compose/v1/analysis/run` + `GET /compose/v1/analysis/{id}/status`.

**Independently verified, not just self-reported**: targeted real test run
(`pytest tests/ -k "compose or analysis_retry"`) → **160 passed, 0 failed**, real Postgres-backed
(testcontainers), 3.75s. `ruff check` on every touched file → clean. Full untargeted local suite not
re-run (known hang risk, `reference-viva-api-local-testcontainers-can-hang.md` — real CI is the right
place for that, once a PR opens).

**Explicit scope choices, flagged by the fork, independently sound on review**: did NOT build PR
#239's own legacy-side half (`analysis.attempt` column/migration, `JobScheduler.update_analysis_retries`)
— that's PR #239's own unmerged scope; touching the legacy `analyses` table would have been real,
unsupervised risk to the path item 1 actually depends on. Built the shared algorithm so PR #239
*could* reuse it later; left legacy wiring alone. `submit_analysis` isn't on the base
`ComposeSimulationService` ABC (Ray/Batch-only, no SLURM equivalent) — real `isinstance` check, clean
400 on other backends, not a silent gap.

**One pre-existing, unrelated finding checked and ruled out**: a live Pyright diagnostic flagged
`database_service.py`'s `insert_package` override parameter rename
(`package_outline`→`package`) as an incompatible override. `git blame` confirms this predates Gap 6
entirely (commit `00d0d2c63`, old `sms_api` naming, well before this work started) — real, minor,
pre-existing tech debt, not introduced by this work, correctly left untouched.

**Gap 6 is DONE. Gaps 1-4 remain genuinely not started** — the larger, foundational piece, and the
actual prerequisite for composite-driven (not caller-supplied-params) analysis dispatch. Branch
pushed, no PR opened (per instruction) — opening one is a real next step whenever this work is picked
back up, not urgent given item 50's non-gating status.

---

## Gaps 1-2 (routing decision) DONE, tested, pushed — Gaps 3-4 genuinely not started, 2026-08-17

Ran concurrently (fork) while the item-60/item-61/item-1 critical path was handled in the main
thread — same pattern as Gap 6. **Resolved the naming-convention-vs-new-CompositeSpec-field
question this file's own Part 1/2/4 leave ambiguous on a surface read**: Part 2's "Proposed
revision plan" (the ORIGINAL gap 1-2 scope) never actually needs a new `CompositeSpec.dispatch`
field — it only reads `CompositeSpec.parameters` for `n_seeds`/`n_generations`, which is
naming-convention-shaped already. The `CompositeSpec.dispatch` sibling field only appears later, in
Part 4/5 (Gap 6's own analysis-GATHER extension) — a different, larger idea for a MORE elaborate
self-description, not something gaps 1-2 need. Alex's "keep going" on the naming-convention
recommendation is honored: no `process_bigraph` changes, at all.

**What's real and tested**: `_resolve_chain_dispatch_spec()` (new,
`simulation_service_ray.py`) resolves a composite through the shared `process_bigraph.composite_spec`
registry — same `get`→on-miss-`discover_specs()`→retry shape `run_pbg.py::_resolve_document` already
uses, not reinvented — and confirms it declares BOTH `n_seeds` and `n_generations` (the naming
convention; `ecoli_baseline`'s own `@composite_generator(parameters={...})` genuinely declares both,
confirmed by reading it directly, not assumed). Wired into `submit_ecoli_simulation_job`'s routing
conditional: on any resolution failure (process_bigraph unavailable in this environment, or the
composite id gone stale the way item 55's bug did), the failure is now logged loudly
(`logger.warning`) rather than silently trusted — **deliberately still dispatches on the pre-existing
`composite is None` + `generations > 1` check either way**, not yet a hard gate. This is a real,
conservative first step: it makes exactly item 55's failure mode observable immediately next time,
without risking breaking a currently-working, production-critical dispatch path on a registry
hiccup. 10 new regression tests (both the resolution helper and the routing conditional's own
integration, including the pre-existing real chain-dispatch test staying green — confirmed
unaffected). Real quality gate run at CI's actual full scope this time (not the narrower single-file
check that caused PR #253's own first CI failure earlier today) — caught and fixed 3 pre-existing
mypy/format issues in Gap 6's OWN files along the way (a `func-returns-value` idiom repeat, a
strict-mode implicit-reexport violation, 7 mock-typing `attr-defined` sites, 3 unformatted files) —
all now clean: full `mypy` (201 files), `ruff check`, `ruff format --check` all pass; 234 real tests
pass (`tests/compose/` + `tests/simulation/test_ray_backend.py`).

**Honest scope correction, worth flagging explicitly**: gap 1 (composite identity/shape sourcing)
and the ROUTING half of gap 2 (whether to chain-dispatch) are what's actually done here. Gap 2's
OTHER half — "the override-KEY VOCABULARY (`cache_dir`, `seed`, the checkpoint triple, etc.) is
hardcoded as literal strings... needs to read them off the resolved spec instead of hardcoding" —
is NOT done. `_seed_generation_command`'s own `overrides` dict is untouched, matching Part 2 point 3
("only the routing decision changes... keep the actual submission mechanism unchanged") — but that
same sentence describes gap 1's scope, and re-reading gap 2's own bullet closely, it's a distinct,
not-yet-done piece. Don't conflate "gaps 1-2 done" with "the override-key hardcoding is gone" —
it isn't yet.

**Gaps 3-4 genuinely not started**: gap 3 (per-commit image concept for the compose subsystem —
compose currently uses one static image tag for the whole deployment; item 1's own dispatch shape
needs a pinned-commit image) and gap 4 (ParCa hardcoded as a non-optional first DAG node) were not
investigated this pass at all — real, substantial, separately-scoped work, not started, not
estimated.

**Commits**: `8650ac8b` (gap 1-2 feature), `47acef90` (quality fixes on Gap 6's files), both on
`feat/item50-compose-native-dispatch`, pushed. No PR opened — held for review, same as everything
else in this item.

---

## Gaps 1-4 STARTED, 2026-08-17 — running concurrently with item 61's deploy/verify chain

Alex, directly: "is there ANYTHING you can do at all... that would be productive to do concurrently?"
— dispatched as a background fork in the same `viva-api-item50` worktree/branch
(`feat/item50-compose-native-dispatch`), explicitly isolated from item 61's own separate worktree
(`viva-api-item50` vs `viva-api-item61` — different checkouts, different branches, no shared
mutating state). Briefed to re-read this file's own Part 2/Part 4 design fresh (not from a
compaction-degraded summary) and stay on the naming-convention approach for composite
dispatch-shape self-description (Alex's "keep going" approval, not the new-`CompositeSpec`-field
alternative — that would be a real contribution to process-bigraph itself, against this ecosystem's
standing rule).

**Explicit, standing constraint, restated directly by Alex and binding regardless of how far this
gets**: **do NOT open a PR or merge anything from this work without Alex reviewing it first.** Same
rule as every PR this session, no exception for this one. The fork was already briefed this way;
this is the durable record of that constraint for any future session picking this up.

Not yet returned as of this checkpoint. Real progress, not yet independently verified — treat
anything reported from it the same as every other fork's claim this session: read the real diff, run
the real tests yourself, don't take the summary at face value.

---

## Fork RETURNED, independently verified — 2026-08-17, post-compaction check

Fork's own report matches this file's "Gaps 1-2... DONE" section above (same content — the fork
wrote that section itself before returning). **Independently re-verified, not taken on the fork's
word**: in the real `viva-api-item50` worktree (`<scratchpad>/viva-api-item50`), `git status` →
clean; `git log --oneline -8` → real commits `47acef90`/`8650ac8b` on top of the 5 Gap-6 commits;
local `HEAD` (`47acef90...`) matches `origin/feat/item50-compose-native-dispatch` exactly via a
fresh `git fetch`. `gh pr list --head feat/item50-compose-native-dispatch --repo
vivarium-collective/viva-api` → **empty, confirmed no PR opened** — the "do not merge without
review" constraint held.

**Standing constraint restated, unchanged**: do NOT open a PR or merge any of this without Alex's
explicit review. Gaps 3-4 remain genuinely not started. This branch is safe to leave exactly as-is
— pushed, no PR, worktree clean — until Alex chooses to review it.
